# WORKLOG — живой журнал проекта BalisticNote Pro

> Этот файл — «подстраховка» для перехода в другой диалог / ветку / аккаунт.
> Здесь главное о веб-версии, синхронизации, деплое и текущих доработках.
> Архитектурный справочник приложения — в [HANDOFF.md](HANDOFF.md). Читай оба.

_Обновляется по ходу работы. Последнее обновление: 2026-06-24._

---

## 1. Ссылки и доступы

| Что | Значение |
|---|---|
| Папка проекта | `/Users/User1/Library/Mobile Documents/com~apple~CloudDocs/Ballistic Calculator/` |
| GitHub-репо | `kulakoffd-gif/ballistic-calculator` (**публичный**, ветка `main`) |
| **Веб-версия (live)** | **https://kulakoffd-gif.github.io/ballistic-calculator/** |
| Эл. почта владельца | kulakoff.d@gmail.com |

**GitHub PAT (токен):** в репозитории НЕ хранится. На каждую сессию пользователь
создаёт новый на https://github.com/settings/tokens (scopes: `repo` + `workflow`)
и передаёт в чат. По окончании работы — удалить токен в том же месте.

---

## 2. Как всё устроено (кратко)

- Приложение — **Capacitor + vanilla JS**, вся логика в папке `www/` (без сборщика).
- Один и тот же код работает как **Android APK** и как **веб-версия (PWA)**.
- Хранилище — **IndexedDB** (`storage.js`), БД `skyrange` **версия 5**.
- Деплой — через GitHub Actions при каждом `git push` в `main`.

---

## 3. Деплой и CI

Два workflow в `.github/workflows/`:
- `build-android.yml` → собирает APK (артефакт).
- `deploy-pages.yml` → публикует `www/` на GitHub Pages (триггер по `www/**`).

**ВАЖНО:** GitHub сериализует Pages-деплои (concurrency group `pages`,
`cancel-in-progress: false`) — при нескольких пушах подряд они идут **по очереди**,
каждый ~1-2 мин. Это нормально.

**Запуск/мониторинг через API** (нужен токен в переменной `$TOKEN`):
```bash
# статус последнего деплоя
curl -s -H "Authorization: token $TOKEN" \
 "https://api.github.com/repos/kulakoffd-gif/ballistic-calculator/actions/workflows/deploy-pages.yml/runs?per_page=1" \
 | python3 -c "import sys,json;r=json.load(sys.stdin)['workflow_runs'][0];print(r['status'],r['conclusion'],r['head_sha'][:7])"

# ручной перезапуск деплоя
curl -s -X POST -H "Authorization: token $TOKEN" -H "Accept: application/vnd.github+json" \
 "https://api.github.com/repos/kulakoffd-gif/ballistic-calculator/actions/workflows/deploy-pages.yml/dispatches" -d '{"ref":"main"}'

# проверить, что свежий код реально на сервере (обходя кэш ?z=)
curl -s "https://kulakoffd-gif.github.io/ballistic-calculator/app.js?z=$(date +%s)" | grep -c "ИСКОМАЯ_СТРОКА"
```

Push с токеном:
```bash
git push "https://kulakoffd-gif:$TOKEN@github.com/kulakoffd-gif/ballistic-calculator.git" main
```

GitHub Pages включён: Settings → Pages → Source = **GitHub Actions** (уже сделано).

---

## 4. Кэш и потеря данных — РЕШЕНО, важные правила

**История проблемы:** был service worker с cache-first → телефон/ноут показывали
старую версию. Я посоветовал чистить «данные сайтов» — это **стёрло базу IndexedDB**
(ошибка с моей стороны).

**Текущее решение (в `index.html`):** service worker **полностью убран**. При заходе
страница сама делает `getRegistrations().unregister()` + чистит **только Cache Storage**
(оболочку), **НЕ трогая IndexedDB/localStorage**. Один раз перезагружается на свежак.

**ПРАВИЛА (повторять пользователю):**
- ✅ Обновить версию: на ноуте **Cmd+Shift+R**; на iPhone — закрыть вкладку и открыть заново.
- ⛔️ **НИКОГДА не советовать чистить «cookie / данные сайтов»** — это удаляет базу
  (полигоны, пули, патроны лежат в IndexedDB).
- Обновления версии саму базу НЕ стирают (IndexedDB переживает обновления; DB
  upgrade v4→v5 только добавляет stores, ничего не удаляет).
- Долговременная страховка данных — **синхронизация с Яндекс.Диском** (Настройки →
  Я.Диск). Один токен на всех устройствах → общие данные + бэкап в облаке.

> Офлайн-режим (PWA) сейчас отключён вместе с SW. Если понадобится — вернуть SW
> с правильным версионированием (см. git-историю sw.js), но это вторично.

---

## 5. Синхронизация (двусторонняя, без потерь)

Реализована в `storage.js` + `backup.js` (подробно в HANDOFF.md §10):
- `Store.mergeAll(payload)` — по-записная склейка (last-write-wins по `updatedAt`),
  удаления через `_tombstones`. `putRaw/delRaw` — без перетирания timestamp.
- `Backup.syncNow()` — скачать Я.Диск → слить → залить обратно. Запускается на старте
  (фоном) и через 2с после изменений. Кнопка «🔄 Синхронизировать» в Настройках.

---

## 6. Главный экран /calc — текущая структура (стиль AB Quantum)

Сверху вниз:
1. **HUD-карточка решения** (sticky): Вертикаль / Горизонталь (mil + MOA + см) со
   стрелками → дистанция (− [N] +) → **строка деталей TOF · Энергия · Скорость · Mach**
   → кнопка «🎯 Промах».
2. **Закреплённый контроллер** (всегда виден, оранжевая рамка): переключатель
   **🧭 Компас / 🕐 Часы** → скорость ветра + виджет направления → кнопки захвата
   **🎯 Навёл на цель / 🌬 Откуда ветер** → V₀ + азимут → кнопки погоды (🌐/📡).
3. Вкладки **Пуля / Атмо / Прочее**.
4. Кнопка **📋 Все дистанции** → отдельный экран `/calc-table` (таблица не висит на главном).
5. Раскрывашки «🔭 Прицел» и «🌡 Атмосфера и баллистика».

### Ветер — конвенция и модель (ВАЖНО при доработке)
- В интерфейсе — **«откуда дует»** (международный стандарт).
- Внутри скрытое поле `windDir` хранит **абсолютное «куда дует»** (TO) — это источник
  правды для солвера: `windAngle_deg = solverWindAngle(windDir, azimuth)`.
- Компас: оранжевая стрелка = «откуда» (= windDir+180 в отображении), серая = азимут цели.
- Часы (`createWindClock`): цель = 12:00 (встречный), 3=справа, 9=слева, 6=попутный.
- Захват компасом (`captureHeading()` + модуль `Compass` в `devices.js`):
  «🎯» пишет в `azimuth_deg`, «🌬» пишет «откуда» → конвертируется в windDir (+180).

### Компас телефона — калибровка
- iOS калибрует магнитометр **автоматически** при движении устройства; отдельной
  «кнопки калибровки» в вебе нет (нельзя вызвать программно).
- Если точность плохая — приложение читает `webkitCompassAccuracy` (модуль `Compass`,
  поле `state.accuracy`) и показывает подсказку: «поводи телефоном восьмёркой».
  Порог «плохо»: `accuracy == null || < 0 || > 20`.
- Мешают: MagSafe/магнитные чехлы, металл рядом. Совет пользователю — снять чехол,
  отойти от машины/железа, сделать «восьмёрку».

---

## 7. Прочие правки этой серии
- GPS: общий помощник `getGeo()` в `app.js` — понятные ошибки (отказ/таймаут) +
  авто-фоллбэк без highAccuracy. Заменил «сырые» вызовы во всех местах.
- Центрирование заголовков: `text-indent` компенсирует хвост `letter-spacing`;
  скрытая кнопка «назад» держит место (`.topbar .ico[hidden]{visibility:hidden}`).

---

## 8. Что дальше (по желанию пользователя, ещё не сделано)
- Вкладки **Solution / Reticle / WEZ прямо в HUD** (сейчас прицел — раскрывашка,
  таблица — отдельный экран; WEZ-вкладки в HUD нет, есть отдельный `/wez`).
- **% вероятности попадания** в HUD (нужен размер цели; логика есть в `/wez`).
- Возможно — синхронизировать конвенцию ветра «откуда» и в визарде `/range/:id/single`
  (сейчас там осталось «куда дует»).

---

## 9. Предпочтения пользователя (важно)
- Язык — русский. Делать самому (не гонять в терминал). Лаконичность.
- Не технарь — объяснять по шагам «куда нажать».
- Референс UI — **Applied Ballistics Quantum** (мануал: см. HANDOFF.md §1,
  путь к PDF в Google Drive). Главный экран — крупное решение сверху, редактируемые
  параметры (ветер/атмо) в зоне большого пальца снизу.
